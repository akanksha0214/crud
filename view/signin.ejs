<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("./partials/head") %>
    <title>Signup Page</title>

    <style>
        body {
           background: url("https://images.unsplash.com/photo-1487014679447-9f8336841d58?auto=format&fit=crop&w=1600&q=80") center/cover no-repeat;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            backdrop-filter: blur(1px);
            margin: 0;
        }

        .page-wrapper {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding-top: 60px; /* prevent overlap under navbar */
        }

        .auth-card {
            background: rgba(255, 255, 255, 0.13);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 32px;
            width: 100%;
            max-width: 390px;
            color: white;
            box-shadow: 0 8px 32px rgba(0,0,0,0.25);
        }

        .auth-title {
            font-size: 1.8rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 20px;
            letter-spacing: -0.02rem;
        }

        .form-control {
            background: rgba(255,255,255,0.25) !important;
            border: none !important;
            border-radius: 10px;
            color: white;
        }

        .form-control::placeholder {
            color: rgba(255,255,255,0.75);
        }

        .btn-auth {
            border-radius: 10px;
            font-weight: 600;
            padding: 10px;
        }

        .auth-footer {
            text-align: center;
            margin-top: 14px;
            font-size: .9rem;
            color: rgba(255,255,255,0.85);
        }

        .auth-footer a {
            color: #ffd95a;
            text-decoration: none;
        }

        .auth-footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>

    <!-- NAVBAR FIXED AT TOP -->
    <%- include("./partials/nav") %>

    <div class="page-wrapper">
        <form action="/user/signin" method="POST">
            <div class="auth-card">
                <div class="auth-title">Enter your Credentials</div>

                <div class="mb-3">
                    <input type="email" class="form-control" name="email" placeholder="Email">
                </div>

                <div class="mb-3">
                    <input type="password" class="form-control" name="password" placeholder="Password">
                </div>

                <button type="submit" class="btn btn-primary btn-auth w-100">
                    Sign In
                </button>

                <div class="auth-footer">
                    Don't have an account? <a href="/user/signup">Sign Up</a>
                </div>
            </div>
        </form>
    </div>

    <%- include("./partials/script") %>
</body>

</html>
